version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: ./build/package/docker/Dockerfile.dev
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - TZ=Asia/Tokyo
    container_name: sasisuseso-maker-dev
